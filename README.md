to-do

handle path variables in router

handle persistent connections, as specified in HTTP 1.1

efficiency (ref/deref, reading chunks rather than 10k block, parallelism, etc)

handle query parameters in router